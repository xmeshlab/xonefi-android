import {describe, expect, test} from '@jest/globals';

import {} from "../../xonefi-api-client/config" 

/**
 * const config = require('../api/config');

function main() {
    console.log(`CONFIG EXISTS: ${config.config_exists()}`);
}
 */

/*config.config_exists() does not exist in the api. Instead exports module.exports = {
    read_default_config,
    write_default_config,
    starter_config,
    config_init_if_absent
};*/
